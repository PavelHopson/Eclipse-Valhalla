// Re-exports for cleaner imports (`types/career` instead of deep service path).
export type {
  Application,
  ApplicationStatus,
  AppSource,
  CareerQuestId,
  CareerQuest,
  CareerStats,
} from '../services/careerService';
